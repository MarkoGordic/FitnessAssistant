<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/openingScreen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor">

    <!-- Opening Screen with the logo -->
    <!-- TODO Add the logo or heartbeat animation -->
    <RelativeLayout
        android:id="@+id/appLogo"

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" >

        <TextView
            android:id="@+id/fitnessTextView"

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:fontFamily="@font/raleway"
            android:includeFontPadding="false"
            android:text="@string/fitness"
            android:textAlignment="center"
            android:textColor="@color/Flame"
            android:textFontWeight="600"
            android:textSize="30sp"/>

        <TextView
            android:id="@+id/assistantTextView"

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:layout_toEndOf="@id/fitnessTextView"

            android:fontFamily="@font/raleway"
            android:includeFontPadding="false"
            android:text="@string/assistant"
            android:textAlignment="center"
            android:textColor="@color/SpaceCadet"
            android:textFontWeight="600"
            android:textSize="30sp"/>

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>